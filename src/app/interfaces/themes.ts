export interface Theme {

    id?: number,
    nomformation?: string,
    domaine?: string,
    theme?: string,
    description?: string,
    prix?: string,
    duree?: string,
    prerequis?: string,
}
