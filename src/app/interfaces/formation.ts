export interface Formation {
    id?: number,
    nomformation?: string,
    domaine?: string,
    theme?: string,
    description?: string,
    prix?: string,
    duree?: string,
    prerequis?: string,
    
}
